/** Papra-ish dark palette. Dark mode only, on purpose. */
export const colors = {
  bg: "#0b0f0e",
  surface: "#131a18",
  surfaceHigh: "#1b2422",
  border: "#26312e",
  text: "#e7edeb",
  textMuted: "#8fa39d",
  primary: "#10b981",
  primaryDim: "#0d9468",
  danger: "#f87171",
  warning: "#fbbf24",
} as const;

export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32 } as const;
export const radius = { sm: 6, md: 10, lg: 16 } as const;
